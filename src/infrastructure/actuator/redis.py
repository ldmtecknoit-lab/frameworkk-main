#start event manager
    #a = [x for x in message if 'redis' in str(x)][0]
    #a.app = constants['identifier']
    #print(a)

#asyncio.get_event_loop().create_task(a.react(domain=constants['identifier']))